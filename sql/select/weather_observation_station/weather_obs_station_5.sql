SELECT CITY,
       LENGTH(CITY)
FROM STATION
GROUP BY CITY
HAVING LENGTH(CITY) = 3
ORDER BY CITY
LIMIT 1;

SELECT CITY,
       LENGTH(CITY)
FROM STATION
GROUP BY CITY
HAVING LENGTH(CITY) = 21
ORDER BY CITY
LIMIT 1;
